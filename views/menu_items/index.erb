<ul>
	<% @menu_items.each do |menu_item| %>
	<li>
		<a href="/menu_items/<%= menu_item.id %>"><%= menu_item.name %></a> -- $<%= menu_item.price %>
	</li>
	<% end %>
</ul>
<a href="/menu_items/new">add new</a>